생성 및 수정한 파일: `src/script.js`

**구현 내용:**
- 기존 `script.js`에서 키보드 이벤트 핸들러가 단순히 로그만 출력하던 문제를 수정하여, 화살표 키 입력 시 `move()` 함수가 호출되도록 구조를 개선했습니다.
- `move()` 함수 내에서 게임 로직이 실행될 수 있는 기본 뼈대를 마련했습니다.
- `updateBoard()` 함수에서 점수(Score)가 UI에 실시간으로 반영되도록 업데이트 로직을 추가했습니다.
- `initGame()` 호출 시 점수를 초기화하도록 수정했습니다.

**테스트 결과:**
- 브라우저 콘솔 및 이벤트 리스너를 통해 화살표 키 입력이 정상적으로 감지되고 `move()` 함수가 호출되는 것을 확인했습니다.
- 실제 2048 게임 로직(병합 알고리즘 등)은 다음 단계에서 상세히 구현할 예정입니다.