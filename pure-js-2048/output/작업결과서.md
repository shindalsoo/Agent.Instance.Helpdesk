작업이 완료되었습니다.

### 생성/수정한 파일 목록
- `src/index.html`: 게임 오버 메시지를 표시할 요소를 추가하고 스타일을 적용했습니다.
- `src/script.js`: 게임 오버 판정 시 `alert` 대신 화면에 'Game Over!' 메시지를 표시하도록 로직을 수정했습니다.

### 구현한 기능 설명
- `src/index.html` 내에 `game-over-message` div 요소를 추가하여 게임오버 시 화면을 덮도록 설정했습니다.
- `src/script.js`의 `isGameOver()` 판정 직후, 기존의 `alert()` 호출을 제거하고 `game-over-message` 스타일을 `flex`로 변경하여 사용자에게 게임 종료 상태를 시각적으로 알리도록 했습니다.
- 새로운 게임 시작(`initGame`) 시 해당 메시지를 다시 숨기도록(`display: none`) 처리했습니다.

### 테스트 결과
- 코드를 직접 실행하여 게임 오버 상황을 연출할 수는 없으나, 코드 로직상 게임 오버 시 UI 업데이트 부분이 정상적으로 호출되도록 수정되었습니다.