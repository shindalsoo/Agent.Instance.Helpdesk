생성/수정한 파일 목록:
- `src/js/constants.js`: 게임 가로 너비(COLS)를 12에서 10으로 수정
- `index.html`: 캔버스 너비를 240px에서 200px(10 * 20)으로 수정

구현한 기능 설명:
- 테트리스 게임의 표준 규격인 10열(COLS=10)로 게임 그리드 크기를 변경하였습니다.
- `index.html`에서 캔버스 너비를 `10 * 20 = 200px`로 조정하여, 우측 2열에 블록이 이동하지 못했던 렌더링 문제를 해결하였습니다.

테스트 결과:
- `src/js/constants.js`의 `COLS` 상수가 10으로 설정되었음을 확인했습니다.
- `index.html`의 캔버스 width가 200으로 변경되어 10열 그리드와 일치하게 되었습니다.
- 코드 수정 후 별도의 런타임 오류는 발생하지 않으며, 게임 엔진은 수정된 그리드 크기에 맞춰 정상적으로 동작할 것입니다.