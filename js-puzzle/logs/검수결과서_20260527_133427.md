작업이 완료되었습니다. `engine.js`와 `main.js`를 수정하여 상하좌우 모든 방향에 대한 이동 및 병합 로직을 구현하였습니다.

### 수정된 파일 목록
- `src/js/engine.js`: `move` 함수를 일반화하여 4방향 처리가 가능하도록 수정, `processLine` 헬퍼 함수 추가.
- `src/js/main.js`: 모든 방향키 입력(ArrowUp, ArrowDown, ArrowLeft, ArrowRight)에 대응하도록 이벤트 핸들러 업데이트.

### 구현한 기능 설명
- **범용 이동 로직**: `processLine` 함수를 통해 행/열 데이터를 추상화하여, 어느 방향으로 이동하든 동일한 병합 규칙(같은 숫자 합치기)이 적용되도록 개선.
- **입력 처리**: 키보드 4방향키를 모두 지원하여 실제 게임 플레이가 가능하도록 개선.

### 테스트 결과
- 브라우저 환경에서 상하좌우 방향키를 모두 사용하여 타일이 이동하고 병합되는 것을 확인했습니다.
- 병합 시 점수가 정상적으로 증가함을 확인했습니다.